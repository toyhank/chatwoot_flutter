# 🎉 Chatwoot Flutter Web 部署成功！

## ✅ 部署信息

**服务器**: `root@43.157.0.135`  
**部署时间**: 2025年12月24日  
**访问地址**: **http://43.157.0.135**

---

## 📦 已完成的工作

### 1. ✅ 构建 Flutter Web 应用
- 使用 `flutter build web --release` 构建生产版本
- 输出目录：`build/web/`
- 构建大小：约 7.5MB（压缩后）

### 2. ✅ 配置 SSH 免密登录
- 生成 SSH 密钥对（RSA 4096位）
- 将公钥上传到服务器
- 测试免密登录成功

### 3. ✅ 上传应用文件
- 打包为 `deploy_package.tar.gz`
- 通过 SCP 上传到服务器
- 解压到 `/var/www/chatwoot_flutter/web/`

### 4. ✅ 安装并配置 Nginx
- 服务器系统：CentOS 7
- Nginx 版本：1.20.1
- 配置文件：`/etc/nginx/conf.d/chatwoot_flutter.conf`
- 启动方式：`systemctl enable nginx && systemctl start nginx`

### 5. ✅ 验证部署
- 内部测试：✅ HTTP 200 OK
- 外部访问：✅ HTTP 200 OK
- 应用可正常访问

---

## 🌐 访问方式

直接在浏览器中打开：

```
http://43.157.0.135
```

---

## 📁 服务器文件结构

```
/var/www/chatwoot_flutter/
├── web/                    # 当前运行版本
│   ├── index.html
│   ├── main.dart.js       # 主应用文件（2.4MB）
│   ├── flutter.js
│   ├── flutter_service_worker.js
│   ├── assets/
│   ├── canvaskit/
│   └── icons/
└── web_backup/            # 备份目录（如有）
```

---

## ⚙️ Nginx 配置说明

### 监听端口
- **HTTP**: 80（默认端口）

### 代理配置
- `/` → Flutter Web 应用（静态文件）
- `/api/v1/` → Chatwoot API（代理到 localhost:3000）
- `/cable` → Chatwoot WebSocket（代理到 localhost:3000）
- `/public/` → Chatwoot 公共资源（代理到 localhost:3000）

### 性能优化
- ✅ Gzip 压缩已启用
- ✅ 静态资源缓存（1年）
- ✅ HTML 文件禁用缓存（保证更新）
- ✅ WebSocket 长连接支持（7天超时）

---

## 🔄 如何更新应用

### 方法1：使用快速部署脚本（推荐）

```batch
# 1. 构建新版本（如有修改）
flutter build web --release --web-renderer html

# 2. 打包
tar -czf deploy_package.tar.gz -C build web

# 3. 执行部署
.\快速部署.bat
```

### 方法2：手动更新

```bash
# 1. 上传新版本
scp deploy_package.tar.gz root@43.157.0.135:/tmp/

# 2. 部署
ssh root@43.157.0.135
cd /var/www/chatwoot_flutter
rm -rf web_backup
mv web web_backup
tar -xzf /tmp/deploy_package.tar.gz
systemctl reload nginx
```

---

## 🛠️ 常用管理命令

### 查看 Nginx 状态
```bash
ssh root@43.157.0.135 "systemctl status nginx"
```

### 重启 Nginx
```bash
ssh root@43.157.0.135 "systemctl restart nginx"
```

### 查看访问日志
```bash
ssh root@43.157.0.135 "tail -f /var/log/nginx/access.log"
```

### 查看错误日志
```bash
ssh root@43.157.0.135 "tail -f /var/log/nginx/error.log"
```

### 测试 Nginx 配置
```bash
ssh root@43.157.0.135 "nginx -t"
```

### 查看应用文件
```bash
ssh root@43.157.0.135 "ls -lh /var/www/chatwoot_flutter/web/"
```

---

## 🔐 安全建议

### 1. 启用 HTTPS（推荐）
```bash
# 安装 Certbot
yum install -y certbot python2-certbot-nginx

# 获取 SSL 证书（需要配置域名）
certbot --nginx -d your-domain.com
```

### 2. 配置云服务器安全组
- 开放端口：80（HTTP）
- 开放端口：443（HTTPS，如需要）
- 限制 SSH 端口（22）仅允许特定IP访问

### 3. 定期备份
```bash
# 备份应用文件
tar -czf chatwoot_flutter_backup_$(date +%Y%m%d).tar.gz /var/www/chatwoot_flutter/

# 备份 Nginx 配置
tar -czf nginx_config_backup_$(date +%Y%m%d).tar.gz /etc/nginx/
```

---

## 📱 应用配置

### 当前配置（lib/config/app_config.dart）

- **主 API**: `http://ccvvb.cn`
- **Chatwoot 服务器**: `http://43.157.0.135:8080`
- **Website Token**: `MDfuT28CS8iMYLdJNXmE95vA`

### 如需修改配置
1. 编辑 `lib/config/app_config.dart`
2. 重新构建：`flutter build web --release`
3. 重新部署

---

## 🎯 下一步建议

1. **配置域名**
   - 将域名解析指向 `43.157.0.135`
   - 修改 Nginx 配置中的 `server_name`
   - 启用 HTTPS

2. **性能优化**
   - 配置 CDN 加速
   - 启用 HTTP/2
   - 优化图片资源

3. **监控告警**
   - 配置服务器监控
   - 设置 Nginx 日志分析
   - 配置磁盘空间告警

4. **备份策略**
   - 定期自动备份应用文件
   - 定期备份数据库（如有）
   - 设置异地备份

---

## 📞 故障排查

### 问题1：无法访问网站
```bash
# 检查 Nginx 是否运行
ssh root@43.157.0.135 "systemctl status nginx"

# 检查端口是否监听
ssh root@43.157.0.135 "netstat -tlnp | grep :80"

# 检查云服务器安全组是否开放80端口
```

### 问题2：页面显示不正常
```bash
# 查看 Nginx 错误日志
ssh root@43.157.0.135 "tail -100 /var/log/nginx/error.log"

# 检查应用文件是否完整
ssh root@43.157.0.135 "ls -lh /var/www/chatwoot_flutter/web/"
```

### 问题3：无法连接 Chatwoot
- 检查 Chatwoot 是否运行在 3000 端口
- 检查 `lib/config/app_config.dart` 中的配置
- 查看浏览器控制台错误信息

---

## 📝 部署脚本清单

项目中已创建以下部署工具：

1. **配置SSH免密登录.bat** - 一键配置SSH免密登录
2. **快速部署.bat** - Windows快速部署脚本
3. **deploy.sh** - Linux/Mac部署脚本
4. **chatwoot_flutter.conf** - Nginx配置文件
5. **部署说明.md** - 详细部署文档
6. **部署成功摘要.md** - 本文档

---

## ✨ 部署成功！

您的 Chatwoot Flutter Web 应用已成功部署到 `43.157.0.135`

现在可以通过浏览器访问：**http://43.157.0.135**

祝您使用愉快！🎊









