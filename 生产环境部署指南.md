# 生产环境部署指南

## 📋 部署前准备

### 1. 配置生产环境参数

在部署前，**必须**先修改 `lib/config/app_config.dart` 中的生产环境配置：

```dart
// 生产环境配置
case Environment.production:
  return 'https://api.yourdomain.com'; // 修改为实际的生产 API 地址
  return 'https://chatwoot.yourdomain.com'; // 修改为实际的生产 Chatwoot 地址
  return 'YOUR_PRODUCTION_TOKEN'; // 修改为实际的生产 Token
  return 'YOUR_PRODUCTION_HMAC_TOKEN'; // 修改为实际的生产 HMAC Token
```

### 2. 服务器要求

- **操作系统**: Ubuntu/Debian Linux
- **Web 服务器**: Nginx（已安装并运行）
- **SSH 访问**: 已配置免密登录
- **防火墙**: 已开放 80 和 443 端口

### 3. 本地环境要求

- Flutter SDK 已安装
- SSH 客户端已配置
- PowerShell（Windows）或 Bash（Linux/Mac）

## 🚀 部署方法

### 方法 1：使用生产环境专用脚本（推荐）

#### Windows 用户

```powershell
# 在项目根目录执行
.\deploy_production.ps1
```

脚本会自动：
1. 确认生产环境配置
2. 使用生产环境参数构建应用
3. 打包并上传到服务器
4. 配置 Nginx 并重启服务

#### Linux/Mac 用户

```bash
# 添加执行权限
chmod +x deploy.sh

# 执行部署（手动指定生产环境）
flutter build web --release --web-renderer html --dart-define=ENV=production
tar -czf deploy_package.tar.gz -C build web
scp deploy_package.tar.gz root@43.157.0.135:/tmp/
# ... 后续步骤参考 deploy.sh
```

### 方法 2：使用通用部署脚本

运行 `deploy.ps1` 或 `deploy.sh`，选择环境时选择"生产环境"：

```powershell
# Windows
.\deploy.ps1
# 然后选择 "2. 生产环境"
```

### 方法 3：手动部署

#### 步骤 1: 构建生产版本

```bash
flutter build web --release --web-renderer html --dart-define=ENV=production
```

#### 步骤 2: 打包构建产物

```bash
tar -czf deploy_package.tar.gz -C build web
```

#### 步骤 3: 上传到服务器

```bash
scp deploy_package.tar.gz root@your-server:/tmp/
```

#### 步骤 4: 在服务器上部署

```bash
ssh root@your-server

# 创建部署目录
mkdir -p /var/www/chatwoot_flutter
cd /var/www/chatwoot_flutter

# 备份旧版本
if [ -d 'web' ]; then
    rm -rf web_backup
    mv web web_backup
fi

# 解压新版本
tar -xzf /tmp/deploy_package.tar.gz

# 设置权限
chmod -R 755 /var/www/chatwoot_flutter
```

#### 步骤 5: 配置 Nginx

```bash
# 编辑或创建 Nginx 配置
nano /etc/nginx/sites-available/chatwoot_flutter.conf
```

Nginx 配置示例：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 修改为您的域名

    root /var/www/chatwoot_flutter/web;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1000;

    # Flutter Web 应用
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control 'no-cache, no-store, must-revalidate';
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control 'public, immutable';
    }

    # Chatwoot API 代理
    location /api/v1/ {
        proxy_pass http://127.0.0.1:3000/api/v1/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Chatwoot WebSocket
    location /cable {
        proxy_pass http://127.0.0.1:3000/cable;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }
}
```

#### 步骤 6: 启用配置并重启 Nginx

```bash
# 创建软链接
ln -sf /etc/nginx/sites-available/chatwoot_flutter.conf /etc/nginx/sites-enabled/

# 删除默认配置（如果存在）
rm -f /etc/nginx/sites-enabled/default

# 测试配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

## 🔒 生产环境安全配置

### 1. 启用 HTTPS（强烈推荐）

使用 Let's Encrypt 获取免费 SSL 证书：

```bash
# 安装 Certbot
apt update
apt install -y certbot python3-certbot-nginx

# 获取证书
certbot --nginx -d your-domain.com

# 自动续期
certbot renew --dry-run
```

### 2. 配置防火墙

```bash
# 允许 HTTP 和 HTTPS
ufw allow 80/tcp
ufw allow 443/tcp

# 启用防火墙
ufw enable

# 查看状态
ufw status
```

### 3. 限制 SSH 访问

```bash
# 禁用 root 密码登录
sed -i 's/#PermitRootLogin yes/PermitRootLogin prohibit-password/' /etc/ssh/sshd_config

# 仅允许密钥认证
sed -i 's/#PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config

# 重启 SSH 服务
systemctl restart sshd
```

### 4. 配置域名解析

在您的域名 DNS 设置中添加 A 记录：

```
类型: A
名称: @ 或 www
值: 您的服务器 IP 地址
TTL: 3600
```

## 📊 部署后验证

### 1. 检查应用是否正常运行

```bash
# 访问应用
curl http://your-domain.com

# 检查 Nginx 状态
systemctl status nginx

# 查看 Nginx 错误日志
tail -f /var/log/nginx/error.log
```

### 2. 检查环境配置

在浏览器中打开应用，检查：
- API 请求是否指向生产服务器
- Chatwoot 连接是否正常
- 所有功能是否正常工作

### 3. 性能测试

```bash
# 使用 curl 测试响应时间
time curl -I http://your-domain.com

# 使用 ab 进行压力测试（需要安装 apache2-utils）
ab -n 1000 -c 10 http://your-domain.com/
```

## 🔄 更新部署

当需要更新应用时：

1. 修改代码并测试
2. 更新 `app_config.dart` 中的配置（如需要）
3. 重新运行部署脚本

```powershell
# Windows
.\deploy_production.ps1
```

旧版本会自动备份到 `web_backup` 目录。

## 🐛 故障排查

### 问题 1: 构建失败

**原因**: 环境参数配置错误

**解决**:
```bash
# 检查 app_config.dart 中的生产环境配置
# 确保所有 URL 和 Token 都已正确设置
```

### 问题 2: 上传失败

**原因**: SSH 连接问题

**解决**:
```bash
# 测试 SSH 连接
ssh root@your-server

# 检查 SSH 密钥配置
ssh-keygen -t rsa -b 4096
ssh-copy-id root@your-server
```

### 问题 3: Nginx 配置错误

**原因**: 配置文件语法错误

**解决**:
```bash
# 测试 Nginx 配置
nginx -t

# 查看详细错误信息
nginx -T
```

### 问题 4: 应用无法访问

**原因**: 防火墙或端口问题

**解决**:
```bash
# 检查端口是否开放
netstat -tlnp | grep :80

# 检查防火墙规则
ufw status

# 检查 Nginx 是否运行
systemctl status nginx
```

## 📝 部署检查清单

部署前请确认：

- [ ] 已修改 `app_config.dart` 中的生产环境配置
- [ ] 已测试生产环境的 API 地址和 Token
- [ ] 服务器已安装 Nginx
- [ ] SSH 免密登录已配置
- [ ] 防火墙规则已配置
- [ ] 域名解析已配置（如使用域名）
- [ ] SSL 证书已配置（如使用 HTTPS）

部署后请验证：

- [ ] 应用可以正常访问
- [ ] API 请求正常
- [ ] Chatwoot 连接正常
- [ ] 所有功能正常工作
- [ ] 错误日志无异常

## 🎯 最佳实践

1. **版本控制**: 使用 Git 管理代码，每次部署前打标签
2. **备份**: 定期备份服务器上的应用文件
3. **监控**: 配置应用监控和日志收集
4. **回滚**: 保留旧版本备份，便于快速回滚
5. **测试**: 在测试环境充分测试后再部署到生产环境

## 📞 技术支持

如遇到问题，请检查：
1. 部署日志输出
2. Nginx 错误日志：`/var/log/nginx/error.log`
3. Nginx 访问日志：`/var/log/nginx/access.log`
4. 服务器系统日志：`journalctl -u nginx`




