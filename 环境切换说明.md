# 环境切换使用说明

## 概述

项目现在支持三种环境配置：
- **开发环境 (development)** - 默认环境，使用本地服务器
- **测试环境 (testing)** - 使用测试服务器
- **生产环境 (production)** - 使用生产服务器

## 切换环境的方法

### 方法 1：使用编译参数（推荐）

在运行或构建 Flutter 应用时，通过 `--dart-define` 参数指定环境：

#### 开发环境（默认）
```bash
flutter run --dart-define=ENV=development
# 或者简写
flutter run --dart-define=ENV=dev
```

#### 测试环境
```bash
flutter run --dart-define=ENV=testing
# 或者简写
flutter run --dart-define=ENV=test
```

#### 生产环境
```bash
flutter run --dart-define=ENV=production
# 或者简写
flutter run --dart-define=ENV=prod
```

### 方法 2：使用 run.bat 脚本（Windows）

运行 `run.bat` 脚本，会提示您选择环境：
1. 选择运行环境（1-3）
2. 选择运行平台（Chrome/Android等）

脚本会自动添加相应的编译参数。

### 方法 3：构建时指定环境

#### Android 构建
```bash
flutter build apk --dart-define=ENV=testing
flutter build appbundle --dart-define=ENV=production
```

#### iOS 构建
```bash
flutter build ios --dart-define=ENV=testing
flutter build ipa --dart-define=ENV=production
```

#### Web 构建
```bash
flutter build web --dart-define=ENV=testing
flutter build web --dart-define=ENV=production
```

## 环境配置说明

各环境的配置在 `lib/config/app_config.dart` 中定义：

### 开发环境
- API 地址: `http://127.0.0.1:8080`
- Chatwoot 地址: `http://127.0.0.1:8080`
- Website Token: `GJFzMx6qnv9DFpaspRpFDRDt`
- HMAC Token: `oZB2ozJPetWF9diBkbrraXxK`

### 测试环境
- API 地址: `http://43.157.0.135:8080`
- Chatwoot 地址: `http://43.157.0.135:8080`
- Website Token: `MDfuT28CS8iMYLdJNXmE95vA`
- HMAC Token: `PxjbshoWpZGwV8hMdchUW84R`

### 生产环境
- API 地址: `https://api.yourdomain.com`（需要修改）
- Chatwoot 地址: `https://chatwoot.yourdomain.com`（需要修改）
- Website Token: `YOUR_PRODUCTION_TOKEN`（需要修改）
- HMAC Token: `YOUR_PRODUCTION_HMAC_TOKEN`（需要修改）

## 修改环境配置

如果需要修改某个环境的配置，请编辑 `lib/config/app_config.dart` 文件中对应的配置值。

## 验证当前环境

在代码中可以通过以下方式检查当前环境：

```dart
import 'package:your_app/config/app_config.dart';

// 检查环境类型
if (AppConfig.isDevelopment) {
  print('当前是开发环境');
} else if (AppConfig.isTesting) {
  print('当前是测试环境');
} else if (AppConfig.isProduction) {
  print('当前是生产环境');
}

// 获取当前环境枚举
Environment env = AppConfig.currentEnvironment;

// 获取当前环境的 API 地址
String apiUrl = AppConfig.baseUrl;
```

## 注意事项

1. **默认环境**：如果不指定 `--dart-define=ENV` 参数，默认使用开发环境
2. **生产环境配置**：部署到生产环境前，请务必修改生产环境的配置值
3. **安全性**：生产环境的 Token 和密钥应该妥善保管，不要提交到代码仓库

