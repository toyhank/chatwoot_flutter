# Chatwoot Flutter Web 部署说明

## 📋 部署要求

### 本地环境
- Flutter SDK 已安装
- SSH 客户端（已配置免密登录到 `root@43.157.0.135`）
- Windows PowerShell（使用 deploy.ps1）或 Bash（使用 deploy.sh）

### 服务器环境
- Ubuntu/Debian Linux
- Nginx 已安装
- Chatwoot 运行在本地 3000 端口（可选）

## 🚀 快速部署

### Windows 用户

```powershell
# 在项目根目录执行
.\deploy.ps1
```

### Linux/Mac 用户

```bash
# 添加执行权限
chmod +x deploy.sh

# 执行部署
./deploy.sh
```

## 📦 部署步骤说明

部署脚本会自动完成以下步骤：

1. **构建 Flutter Web 应用**
   - 使用 `flutter build web --release` 构建生产版本
   - 输出目录：`build/web/`

2. **打包构建产物**
   - 将 `build/web/` 打包为 `deploy_package.tar.gz`

3. **上传到服务器**
   - 上传打包文件到 `/tmp/chatwoot_flutter_deploy/`
   - 上传 Nginx 配置文件

4. **服务器配置**
   - 部署目录：`/var/www/chatwoot_flutter/`
   - 备份旧版本（如果存在）
   - 解压新版本
   - 配置 Nginx 反向代理
   - 重启 Nginx 服务

5. **清理临时文件**
   - 删除本地和服务器上的临时文件

## 🌐 访问地址

部署完成后，通过以下地址访问：

```
http://43.157.0.135
```

## 🔧 服务器配置详情

### Nginx 配置位置
- 配置文件：`/etc/nginx/sites-available/chatwoot_flutter.conf`
- 软链接：`/etc/nginx/sites-enabled/chatwoot_flutter.conf`

### 应用部署位置
- Web 文件：`/var/www/chatwoot_flutter/web/`
- 备份目录：`/var/www/chatwoot_flutter/web_backup/`（如果存在）

### 代理配置
- `/` → Flutter Web 应用
- `/api/v1/` → Chatwoot API（代理到 localhost:3000）
- `/cable` → Chatwoot WebSocket（代理到 localhost:3000）
- `/public/` → Chatwoot 公共资源（代理到 localhost:3000）

## 🐛 故障排查

### 查看 Nginx 错误日志
```bash
ssh root@43.157.0.135 'tail -f /var/log/nginx/error.log'
```

### 查看 Nginx 访问日志
```bash
ssh root@43.157.0.135 'tail -f /var/log/nginx/access.log'
```

### 测试 Nginx 配置
```bash
ssh root@43.157.0.135 'nginx -t'
```

### 重启 Nginx
```bash
ssh root@43.157.0.135 'systemctl restart nginx'
```

### 查看 Nginx 状态
```bash
ssh root@43.157.0.135 'systemctl status nginx'
```

### 检查端口占用
```bash
ssh root@43.157.0.135 'netstat -tlnp | grep :80'
```

## 🔄 重新部署

如果需要更新应用，只需再次运行部署脚本即可：

```powershell
# Windows
.\deploy.ps1
```

```bash
# Linux/Mac
./deploy.sh
```

旧版本会自动备份到 `web_backup` 目录。

## ⚙️ 环境配置

### 修改后端 API 地址

编辑 `lib/config/app_config.dart`：

```dart
// 主 API 服务器
static const String baseUrl = 'http://your-api-server.com';

// Chatwoot 服务器
static const String chatwootBaseUrl = 'http://43.157.0.135';
```

修改后需要重新构建和部署。

### 修改 Chatwoot Token

编辑 `lib/config/app_config.dart`：

```dart
static const String chatwootWebsiteToken = 'your-website-token';
```

## 🔐 安全建议

1. **启用 HTTPS**
   - 使用 Let's Encrypt 获取免费 SSL 证书
   - 配置 Nginx SSL

2. **配置防火墙**
   ```bash
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw enable
   ```

3. **限制 SSH 访问**
   - 禁用 root 密码登录
   - 仅允许密钥认证

## 📝 注意事项

1. 确保服务器已安装 Nginx：
   ```bash
   ssh root@43.157.0.135 'apt update && apt install -y nginx'
   ```

2. 如果 Chatwoot 使用其他端口，需要修改 Nginx 配置中的代理地址

3. 首次部署可能需要配置服务器防火墙规则

4. 建议定期备份 `/var/www/chatwoot_flutter/` 目录

## 📞 技术支持

如遇到问题，请检查：
1. SSH 免密登录是否正常
2. Flutter SDK 版本是否符合要求
3. 服务器 Nginx 是否正常运行
4. 网络连接是否畅通

## 🎯 下一步

部署成功后，您可以：
1. 配置域名解析
2. 启用 HTTPS
3. 配置 CDN 加速
4. 设置监控和日志分析









