# Firebase 推送失败诊断清单

## 第一步：确认应用是否成功启动

在应用启动时，查看 Flutter 日志（在运行 `flutter run` 的终端窗口中），确认以下关键信息：

### ✅ 必须出现的日志

```
✅ Firebase 初始化成功
✅ 用户已授予推送权限
📱 FCM Token: [完整的token字符串]
💾 FCM Token 已保存到本地存储
✅ 推送服务初始化成功
✅ Firebase 和推送服务初始化完成
```

### ❌ 如果没有看到这些日志

**问题 1：没有看到任何 Firebase 相关日志**
- **原因**：可能是热重载，Firebase 初始化只在应用首次启动时执行
- **解决**：完全停止应用（在 flutter run 终端按 `q` 退出），然后重新运行：
  ```bash
  flutter run --dart-define=ENV=development
  ```

**问题 2：看到 Firebase 初始化失败错误**
- 检查 `google-services.json` 文件是否在正确位置：`android/app/google-services.json`
- 确认包名是否匹配：应用包名 `com.card` = google-services.json 中的 `package_name`
- 尝试 `flutter clean` 后重新构建

---

## 第二步：获取 FCM Token

### 从日志中复制 Token

在应用启动后，查找类似这样的日志：
```
📱 FCM Token: eA7xGk...很长的字符串...x9Pz
```

**复制完整的 Token**（通常有 140-180 个字符）

### 验证 Token 是否有效

- Token 应该以字母开头
- 包含大小写字母、数字、下划线、连字符  
- 长度约 140-180 字符

---

## 第三步：检查通知权限

### Android 13+ (API 33+)

如果您的设备是 Android 13 或更高版本，需要手动授予通知权限：

1. 打开设备的 **设置** → **应用** → **com.card**（或应用名称）
2. 找到 **通知** 或 **Notifications**
3. 确保通知已**允许**或**开启**

### 日志中的权限状态

查找这行日志：
- `✅ 用户已授予推送权限` - 权限正常
- `❌ 用户拒绝了推送权限` - 需要手动在设置中开启
- `⚠️ 用户授予了临时推送权限` - iOS 专有，Android 无此状态

---

## 第四步：Firebase Console 发送测试

### 4.1 访问 Firebase Console

1. 打开 https://console.firebase.google.com/
2. 选择项目：**xcard-2b2ea**

### 4.2 发送测试消息

1. 左侧菜单 → **Engage** → **Messaging（消息传递）**
2. 点击 **Create your first campaign** 或 **New campaign**
3. 选择 **Firebase Notification messages**

### 4.3 填写通知内容

- **Notification title**（标题）：`测试推送`
- **Notification text**（正文）：`这是测试消息`
- **Notification image**（可选）：留空

### 4.4 **重要**：先发送测试消息

**不要直接点 Next！** 而是：

1. 在当前页面底部找到 **Send test message**
2. 点击后会弹出对话框
3. 输入您复制的 **FCM registration token**（完整的 Token）
4. 点击 **Test** 按钮

### 4.5 常见错误

#### 错误：Invalid registration token

- **原因**：Token 复制不完整或包含额外字符
- **解决**：重新从日志复制完整 Token，确保没有多余空格或换行

#### 错误：Requested entity was not found

- **原因**：Token 与项目不匹配
- **解决**：确认正在使用正确的 Firebase 项目，检查 `google-services.json` 中的 `project_id` 是否为 `xcard-2b2ea`

#### 提示：Successfully sent message

- **成功**：说明服务端已发送推送
- **下一步**：检查设备是否收到

---

## 第五步：验证设备接收

### 应用在前台时

- 查看 Flutter 日志，应显示：
  ```
  📩 收到前台消息:
    - 标题: 测试推送
    - 内容: 这是测试消息
  ```
- **注意**：Android 默认**前台不显示通知栏**，只会触发回调

### 应用在后台时

- 通知应显示在 Android 通知栏（下拉查看）
- 点击通知后，日志会显示：
  ```
  📬 后台消息被点击:
    - 标题: 测试推送
  ```

### 应用完全关闭时

- 通知应显示在通知栏
- 点击后应用会启动
- 日志会显示：
  ```
  🌙 后台消息处理:
    - 标题: 测试推送
  ```

---

## 常见问题排查

### ❌ 问题：收不到任何推送

#### 检查清单：

1. **Firebase 初始化成功了吗？**
   - 查看应用启动日志，确认 `✅ Firebase 初始化成功`

2. **FCM Token 获取成功了吗？**
   - 查看日志，确认 `📱 FCM Token: ...`
   - Token 不应为空或 null

3. **通知权限授予了吗？**
   - 进入系统设置检查应用通知权限
   - Android 13+ 必须手动授权

4. **网络连接正常吗？**
   - FCM 需要网络连接
   - 检查模拟器/设备的网络状态

5. **使用了正确的 Token 吗？**
   - 复制最新的完整 Token
   - 确保没有多余空格或特殊字符

6. **Firebase 项目配置正确吗？**
   - 检查 `google-services.json` 中的 `package_name` 是否为 `com.card`
   - 检查 `project_id` 是否为 `xcard-2b2ea`

---

### ❌ 问题：前台收不到推送

**这是正常行为！**

- Android 在应用位于前台时，默认**不显示通知栏通知**
- 推送会触发 `onMessage` 回调（查看日志中的 `📩 收到前台消息`）
- 如需在前台也显示通知，需要在代码中手动创建本地通知

**验证方法**：
1. 按设备的 Home 键，让应用进入后台
2. 再次发送测试推送
3. 查看通知栏是否出现

---

### ❌ 问题：后台也收不到推送

#### 可能原因 1：设备省电模式

- 某些设备（如小米、华为）的省电模式会阻止后台推送
- **解决**：在设备设置中将应用加入白名单

#### 可能原因 2：Google Play Services 未安装

- FCM 依赖 Google Play Services
- 如果是国内设备或某些模拟器，可能没有 Google 服务
- **解决**：使用安装了 Google Play 的模拟器（如 Pixel 系列）

#### 可能原因 3：Firebase 配置问题

- 运行 `flutter clean` 后重新构建
- 确认 `google-services.json` 配置正确

---

## 快速诊断命令

### 查看完整应用日志

在 PowerShell 中运行：
```powershell
flutter logs
```
然后查找包含 "Firebase"、"FCM"、"Token" 的行

### 重新安装应用（清除所有数据）

```bash
flutter clean
flutter run --dart-define=ENV=development
```

### 检查 Firebase 项目配置

打开 `android/app/google-services.json`，确认：
```json
{
  "project_info": {
    "project_id": "xcard-2b2ea"  // ✅ 必须匹配
  },
  "client": [{
    "client_info": {
      "android_client_info": {
        "package_name": "com.card"  // ✅ 必须匹配
      }
    }
  }]
}
```

---

## 下一步

如果按照以上步骤仍然无法接收推送，请提供：

1. **应用启动时的完整日志**（从 "Launching lib\main.dart" 到启动完成）
2. **Firebase Console 发送测试消息后的响应**（成功还是失败）
3. **设备/模拟器信息**（Android 版本，是否有 Google Play Services）
4. **通知权限状态**（在系统设置中查看）

我会根据这些信息进一步诊断问题。
