# Firebase 推送通知测试指南

## 测试步骤

### 第 1 步：检查 Firebase 初始化

应用启动后，查看日志输出，确认以下信息：

✅ **成功标志：**
```
✅ Firebase 初始化成功
✅ 用户已授予推送权限
📱 FCM Token: [完整token字符串]
💾 FCM Token 已保存到本地存储
✅ 推送服务初始化成功
✅ Firebase 和推送服务初始化完成
```

⚠️ **如果看到错误：**
- 检查包名是否匹配（应为 `com.card`）
- 确认 `google-services.json` 在正确位置
- 尝试完全卸载应用重新安装

---

### 第 2 步：获取 FCM Token

**方法 1：从日志中复制**
在应用启动日志中查找：
```
📱 FCM Token: [这里是您的token]
```

**方法 2：打开客服页面**
1. 在应用中打开客服页面
2. 查看日志，会显示：
```
📤 开始发送 FCM Token 到 Chatwoot...
📱 FCM Token: [token前20字符]...
✅ FCM Token 已发送到 Chatwoot
```

---

### 第 3 步：使用 Firebase Console 发送测试推送

#### 3.1 访问 Firebase Console
1. 打开浏览器访问：https://console.firebase.google.com/
2. 选择您的项目：**xcard-2b2ea**

#### 3.2 发送测试消息
1. 在左侧菜单中找到 **Messaging**（消息传递）
2. 点击 **Create your first campaign**（创建第一个广告系列）或 **New campaign**
3. 选择 **Firebase Notification messages**

#### 3.3 编写通知内容
在 **Notification** 部分：
- **Notification title**（标题）：`测试推送`
- **Notification text**（内容）：`这是来自 Firebase 的测试推送通知`
- **Notification image**（图片）：可选

#### 3.4 选择目标设备
1. 点击 **Next**
2. 在 **Target** 部分选择：
   - **User segment**: Select an app
   - 选择您的应用：**com.card** (Android)
3. 点击 **Next**

#### 3.5 发送测试
在发送前，建议先发送测试：
1. 点击 **Send test message**
2. 输入您从日志中复制的 **FCM registration token**
3. 点击 **Test** 按钮

---

### 第 4 步：验证推送接收

#### 应用在前台时
- 查看日志输出：
```
📩 收到前台消息:
  - 标题: 测试推送
  - 内容: 这是来自 Firebase 的测试推送通知
```

#### 应用在后台时
- 通知会显示在 Android 通知栏
- 点击通知会触发：
```
📬 后台消息被点击:
  - 标题: 测试推送
  - 内容: 这是来自 Firebase 的测试推送通知
```

#### 应用完全关闭时
- 通知会显示在 Android 通知栏
- 后台处理器会记录：
```
🌙 后台消息处理:
  - 标题: 测试推送
  - 内容: 这是来自 Firebase 的测试推送通知
```

---

## 使用 ADB 命令测试（高级）

如果您想直接通过命令行测试，可以使用 Firebase Admin SDK 或 curl：

```bash
# 需要先获取 Server Key（在 Firebase Console → Project Settings → Cloud Messaging）
curl -X POST https://fcm.googleapis.com/fcm/send \
  -H "Authorization: key=YOUR_SERVER_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "YOUR_FCM_TOKEN",
    "notification": {
      "title": "测试推送",
      "body": "通过 API 发送的测试消息"
    }
  }'
```

---

## 调试技巧

### 查看 Firebase 详细日志
```bash
# Android 日志过滤
adb logcat | findstr /i "firebase"
adb logcat | findstr /i "fcm"

# 或在 PowerShell 中
adb logcat | Select-String -Pattern "Firebase|FCM"
```

### 查看应用权限状态
```bash
adb shell dumpsys package com.card | findstr permission
```

### 清除应用数据重新测试
```bash
adb shell pm clear com.card
```

---

## 常见问题

### ❌ 收不到推送通知

**检查清单：**
1. ✅ 通知权限已授予？
2. ✅ FCM Token 是否成功获取？
3. ✅ Firebase 初始化是否成功？
4. ✅ google-services.json 配置是否正确？
5. ✅ 网络连接是否正常？

**解决方案：**
- 在 Android 设置中手动检查应用的通知权限
- 重新安装应用
- 检查 Firebase Console 中的项目配置

### ⚠️ Token 获取失败

如果日志显示 "FCM Token 为空"：
1. 检查 `google-services.json` 是否存在
2. 确认包名 `com.card` 与 Firebase 配置匹配
3. 运行 `flutter clean` 后重新构建

---

## 测试完成后

一旦推送测试成功，您可以：
1. 在 Chatwoot 服务端集成 Firebase Admin SDK
2. 在会话更新、新消息等事件时发送推送
3. 自定义推送内容和点击行为
