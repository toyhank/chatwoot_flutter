# 推送测试问题诊断

## 当前状态检查

### 确认事项
- ✅ 通知权限已打开：POST_NOTIFICATIONS: granted=true
- ✅ 应用正在运行：进程 ID 6260
- ⚠️ 无法找到 Flutter 日志输出

##  可能的问题

### 1. 应用可能未正确重启
Flutter 的热重启可能没有重新初始化 Firebase。

**解决方案**：完全重启应用
```bash
# 在 flutter run 终端按 q 键退出
# 然后重新运行
flutter run --dart-define=ENV=development
```

### 2. Firebase 初始化可能失败但无日志
某些 Android 模拟器可能不支持 Google Play Services。

**检查方法**：
```bash
adb logcat -d | Select-String -Pattern "Firebase|GMS"
```

### 3. 使用了旧的 Token
如果使用了之前的 Token（权限关闭时获取的），推送会失败。

**解决方案**：获取新的 Token

---

## 建议的测试步骤

### 步骤 1：完全重启应用

1. 在 flutter run 终端按 `q` 键完全退出
2. 清除日志：`adb logcat -c`
3. 重新运行：`flutter run --dart-define=ENV=development`
4. 等待应用启动完成

### 步骤 2：获取新的 FCM Token

应用启动后，立即查看日志：
```bash
adb logcat | Select-String -Pattern "I/flutter"
```

应该看到：
```
✅ Firebase 初始化成功
✅ 用户已授予推送权限
📱 FCM Token: [新的token]
```

### 步骤 3：确认应用在后台

**重要**：推送测试前必须：
1. 按设备的 **Home 键**（不是返回键）
2. 应用进入后台但仍在运行
3. 然后发送推送

### 步骤 4：使用新 Token 测试

在 Firebase Console:
1. 进入 Messaging
2. Send test message
3. 粘贴新获取的完整 Token
4. 点击 Test

---

## 快速诊断命令

### 查看 Firebase 相关日志
```bash
adb logcat -d | Select-String -Pattern "FirebaseApp|FirebaseMessaging" | Select-Object -Last 20
```

### 查看应用是否收到推送
```bash
adb logcat | Select-String -Pattern "GCM|FCM"
```

### 查看通知权限状态
```bash
adb shell dumpsys package com.card | Select-String -Pattern "POST_NOTIFICATIONS"
```
